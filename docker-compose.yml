version: '3.8'

services:
  quant-flow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quant-flow-bot
    restart: unless-stopped

    # Environment variables (override with .env file)
    environment:
      # LLM Client Configuration
      # OpenAI/DeepSeek API Configuration
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_API_BASE=${OPENAI_API_BASE:-https://api.deepseek.com/v1}
      # Cloudflare Workers AI Configuration
      - CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID}
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN}
      # Google Gemini Configuration
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      # LiteLLM Configuration
      - LITELLM_API_BASE=${LITELLM_API_BASE}
      - LITELLM_API_KEY=${LITELLM_API_KEY}
      # NVIDIA AI Endpoints Configuration
      - NVIDIA_API_KEY=${NVIDIA_API_KEY}

      # Exa Search API Configuration (Optional)
      - EXA_API_KEY=${EXA_API_KEY:-}

      # Hyperliquid Configuration
      - HYPERLIQUID_PRIVATE_KEY=${HYPERLIQUID_PRIVATE_KEY}
      - HYPERLIQUID_ACCOUNT_ADDRESS=${HYPERLIQUID_ACCOUNT_ADDRESS:-}
      - HYPERLIQUID_TESTNET=${HYPERLIQUID_TESTNET:-true}

      # Application Configuration
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - TRADE_AMOUNT=${TRADE_AMOUNT:-}
      - TAKE_PROFIT_RATIO=${TAKE_PROFIT_RATIO:-}
      - STOP_LOSS_RATIO=${STOP_LOSS_RATIO:-}
      - DEFAULT_LEVERAGE=${DEFAULT_LEVERAGE:-}

    # Mount volumes for configuration and persistence
    volumes:
      # Configuration file (read-only)
      - ./config.yaml:/app/config.yaml:ro

      # Prompts directory (read-only)
      - ./prompts:/app/prompts:ro

      # Logs directory (persistent, read-write)
      # IMPORTANT: Run ./init-deployment.sh first to set correct permissions
      # Or manually: chmod -R 777 ./logs
      - ./logs:/app/logs

      # Optional: Mount .env file if you prefer file-based config
      # - ./.env:/app/.env:ro

    # Resource limits (adjust based on your needs)
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 256M

    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

    # Network configuration
    networks:
      - quant-network

    # Security options
    security_opt:
      - no-new-privileges:true

    # Read-only root filesystem (except mounted volumes)
    # Commented out as the app may need to write temporary files
    # read_only: true

    # Health check（slim 镜像无 pgrep，使用 python 检测进程）
    healthcheck:
      test: ["CMD", "python", "-c", "import os,sys; pids=[p for p in os.listdir('/proc') if p.isdigit()]; cmds=[open(f'/proc/{p}/cmdline').read() for p in pids if os.path.exists(f'/proc/{p}/cmdline')]; sys.exit(0 if any('main.py' in c for c in cmds) else 1)"]
      interval: 60s
      timeout: 10s
      start_period: 60s
      retries: 3

networks:
  quant-network:
    driver: bridge
