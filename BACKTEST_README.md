# 回测系统使用说明

## 概述

回测系统允许您使用真实历史数据测试交易模型的成功率，无需实际执行交易。系统支持从Hyperliquid API获取历史数据或使用本地数据文件。

## 功能特性

- ✅ 使用真实历史K线数据回测
- ✅ 支持从Hyperliquid API获取历史数据
- ✅ 支持从本地CSV/JSON文件加载数据
- ✅ 模拟完整的交易流程（开仓、平仓、止盈止损）
- ✅ 计算手续费和滑点
- ✅ 生成详细的回测报告（JSON + CSV）
- ✅ 统计关键指标（胜率、盈亏比、最大回撤等）

## 使用方法

### 1. 从API获取历史数据回测

```bash
python backtest.py --symbol BTC --start-date 2025-11-01 --end-date 2025-12-29
```

### 2. 使用本地数据文件回测

```bash
python backtest.py --symbol BTC --data-file data/btc_history.csv
```

### 3. 指定初始余额和决策间隔

```bash
python backtest.py --symbol ETH \
    --start-date 2024-06-01 \
    --end-date 2024-12-01 \
    --initial-balance 10000 \
    --interval 15
```

## 命令行参数

### 必需参数

- `--symbol`: 交易对符号（如 BTC, ETH）
- `--data-file` 或 `--start-date`: 数据源（二选一）

### 可选参数

- `--end-date`: 结束日期（格式: YYYY-MM-DD，与--start-date一起使用）
- `--timeframe`: K线时间周期（默认: 15m）
- `--initial-balance`: 初始余额（USD，默认: 10000）
- `--interval`: 决策间隔（分钟，默认: 15）
- `--output-dir`: 报告输出目录（默认: backtest_results）
- `--config`: 配置文件路径（默认: config.yaml）
- `--testnet`: 使用测试网（仅用于API数据源）

## 数据文件格式

如果使用本地数据文件，文件必须包含以下列：

- `timestamp`: 时间戳（支持datetime字符串或毫秒时间戳）
- `open`: 开盘价
- `high`: 最高价
- `low`: 最低价
- `close`: 收盘价
- `volume`: 成交量

### CSV示例

```csv
timestamp,open,high,low,close,volume
2024-01-01 00:00:00,42000.0,42500.0,41800.0,42200.0,1000.5
2024-01-01 00:15:00,42200.0,42800.0,42100.0,42600.0,1200.3
```

### JSON示例

```json
[
  {
    "timestamp": "2024-01-01T00:00:00",
    "open": 42000.0,
    "high": 42500.0,
    "low": 41800.0,
    "close": 42200.0,
    "volume": 1000.5
  }
]
```

## 回测报告

回测完成后，系统会生成以下文件：

1. **JSON报告** (`backtest_{symbol}_{timestamp}.json`): 包含完整的回测结果和所有交易记录
2. **CSV报告** (`backtest_{symbol}_{timestamp}.csv`): 包含每笔交易的明细

### 报告指标说明

- **总盈亏**: 所有交易的累计盈亏
- **总收益率**: 总盈亏 / 初始余额
- **胜率**: 盈利交易数 / 总交易数
- **盈亏比**: 平均盈利 / 平均亏损
- **最大回撤**: 账户价值从峰值到谷值的最大跌幅

## 注意事项

1. **API限制**: 从Hyperliquid API获取数据时，请注意API的速率限制
2. **数据质量**: 确保历史数据完整且准确，缺失数据可能影响回测结果
3. **手续费**: 系统使用Hyperliquid的标准手续费率（0.035% per side）
4. **滑点**: 回测中使用收盘价作为成交价，实际交易可能存在滑点
5. **资金费率**: 当前版本暂不考虑资金费率的影响

## 示例输出

```
📊 回测报告摘要
============================================================

💰 账户信息:
   初始余额: $10000.00
   最终余额: $10500.00
   总盈亏: $500.00
   总收益率: 5.00%
   总手续费: $35.00

📈 交易统计:
   总交易数: 10
   盈利交易: 6
   亏损交易: 4
   胜率: 60.00%

💵 盈亏分析:
   平均盈利: $150.00
   平均亏损: $100.00
   盈亏比: 1.50

📉 风险指标:
   最大回撤: 2.50%
```

## 故障排除

### 问题：无法从API获取数据

- 检查网络连接
- 确认交易对符号正确
- 尝试使用 `--testnet` 参数

### 问题：本地文件加载失败

- 检查文件路径是否正确
- 确认文件格式符合要求
- 检查文件编码是否为UTF-8

### 问题：回测结果异常

- 检查历史数据是否完整
- 确认配置文件中的参数设置合理
- 查看日志文件了解详细错误信息

