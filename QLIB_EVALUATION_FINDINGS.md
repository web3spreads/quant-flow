# QLib 信号评估发现与待解决问题

## 评估日期: 2026-03-12

## 一、评估概述

使用 Walk-Forward 方法评估 QLib 模型信号质量。
- 训练集 70%（2024-03 ~ 2025-08），验证集 15%，测试集 15%（2025-11 ~ 2026-03）
- 交易成本: 0.05% 单边手续费
- 测试了信号阈值过滤（Q0.3/0.5/0.7）、信号强度仓位、不同持仓周期（1/2/3期）

## 二、核心发现

### 1. BTC：模型完全失效（所有配置 IC=0）
- **现象**: 所有模型（LightGBM/Linear/ElasticNet）对 BTC 4h 数据预测值全为 0 或近似常数
- **根因**: BTC 价格收益率的信噪比极低，特征与标签相关性接近 0，模型正则化后退化为常数预测
- **方向准确率**: ~50%（等于随机）
- **结论**: BTC 的 QLib 信号当前**没有价值**

### 2. ETH：最佳表现，信号有辅助价值
- **最佳配置**: `1h_l12_all`（1小时频率，预测未来12小时，全部特征，LightGBM）
  - 测试集 IC=0.0746, Rank_IC=0.0602
  - 全量方向准确率=52.8%
  - Q0.5 过滤后方向准确率=**54.8%**
  - 最佳策略: Weighted 持仓1期 Q0.7 → 夏普=2.18, 总收益=10.1%, 最大回撤=-5.8%
- **次佳配置**: `4h_l3_all`（4小时频率，预测12小时，全部特征，LightGBM）
  - IC=0.0331
  - Q0.7 过滤后 Sign 策略 → 夏普=2.05, 总收益=20.5%, 最大回撤=-10.9%
- **结论**: ETH 信号作为 LLM 辅助输入**有价值**（方向准确率 >50%）

### 3. SOL：信号较弱，过滤后有边际价值
- **最佳配置**: `4h_l5_k20`（4小时频率，预测20小时，top20特征，LightGBM）
  - IC=0.0683, Rank_IC=0.1130（IC 值不错但方向准确率不高）
  - 全量方向准确率=46.9%（低于50%）
  - Q0.3 过滤后 Sign 策略 → 夏普=1.45, 总收益=21.0%, 但最大回撤=-37.2%（过大）
- **结论**: SOL 信号质量不稳定，回撤过大，仅作为参考

## 三、发现的关键问题

### 问题 1: 默认正则化参数过强，导致模型退化
- **严重程度**: 高
- **描述**: `trainer.py` 中 LightGBM 默认 `lambda_l1=50, lambda_l2=50` 过强，加上 `min_child_samples=50`，导致模型在验证集上第 1 轮就 early stopping，预测值退化为常数 0
- **影响**: BTC 和大部分 top_k=10 配置完全失效
- **建议**: 降低正则化强度。回测中使用 `lambda_l1=1.0, lambda_l2=1.0, min_child_samples=20` 效果明显改善
- **文件**: `/home/user/quant-flow/src/qlib_engine/model/trainer.py` 第 44-56 行

### 问题 2: top_k=10 特征选择过于激进
- **严重程度**: 中
- **描述**: 只用 10 个特征时，信息量不足以支撑有效预测。使用全部特征（top_k=0）或 top_k=20 效果显著提升
- **影响**: BTC/SOL 在 top_k=10 时 IC 为 0，增加到全部特征后 SOL IC 达到 0.0487
- **建议**: 默认 `feature_select_top_k` 设为 0 或 20 以上
- **文件**: 配置在 `config.yaml` 的 `qlib.data.feature_select_top_k`

### 问题 3: BTC 4h 数据量不足以训练有效模型
- **严重程度**: 中
- **描述**: 4h 频率下 BTC 约 4381 条数据（训练集 ~3066 条），信噪比低，模型无法学到有效模式
- **建议**:
  - 尝试 1h 频率增加数据量
  - 或者对 BTC 直接禁用 QLib 信号，减少噪声干扰
  - 或尝试混合训练（多币种数据一起训练）

### 问题 4: ElasticNet 对加密货币数据表现极差
- **严重程度**: 低
- **描述**: ElasticNet 在几乎所有配置下预测值为 0（alpha=0.1 仍然过强）
- **建议**: 从默认候选模型列表中移除 ElasticNet，或大幅降低 alpha（如 0.001）

### 问题 5: sign(prediction) 策略不适合实际使用
- **严重程度**: 信息
- **描述**: 简单 sign 策略收益波动大（BTC -86%），但 Weighted 策略（信号强度调仓）在 ETH 上表现合理（夏普 2.18，回撤 -5.8%）
- **结论**: QLib 信号应作为 LLM 决策的辅助输入而非独立策略，这一定位是正确的

## 四、下一步建议

1. **调整 trainer.py 默认参数**: 降低 LightGBM 正则化强度
2. **调整默认 top_k**: 从 10 改为 0（不做特征选择）或 20
3. **BTC 专项优化**: 尝试 1h 频率 + 更长标签期数，或考虑禁用 BTC 的 QLib 信号
4. **ETH 生产配置**: 推荐使用 `1h, label_periods=12, top_k=0`
5. **SOL 生产配置**: 推荐使用 `4h, label_periods=5, top_k=20`
6. **移除 ElasticNet**: 从默认候选模型列表中删除
7. **LLM 提示词调整**: 根据各币种信号可靠性，在提示词中区分信号权重

## 五、回测脚本

评估脚本: `/home/user/quant-flow/evaluate_qlib_signals.py`
详细结果 CSV: `/home/user/quant-flow/qlib_signal_evaluation_results.csv`
